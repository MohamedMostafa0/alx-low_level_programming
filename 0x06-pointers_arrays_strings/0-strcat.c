#include "main.h"
#include "string.h"
/**
 * _strcat - func
 * @dest: a - Variable
 * @src: b - variable
 * Return: return
 */
char *_strcat(char *dest, char *src)
{
	strcpy(dest, s);
	return (dest);
}

